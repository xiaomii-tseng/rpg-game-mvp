import { Routes } from '@angular/router';
import { CombatViewComponent } from './features/combat/combat-view.component';

export const routes: Routes = [
  { path: '', component: CombatViewComponent }, // 首頁直接進戰鬥
];